# Server configuration
PORT=3000

# The public URI for your application. This must match the redirect URI
# configured in both your Discord and XAuthConnect applications.
# The path should be the specific callback endpoint.
REDIRECT_URI="http://localhost:3000"

# Database connection
DB_HOST=localhost
DB_USER=postgres
DB_PASSWORD=password
DB_DATABASE=discord_roles

